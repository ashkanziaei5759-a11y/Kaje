# استقرار روی Vercel با دیتابیس رایگان

## چرا این مراحل دستی است

پروژه به یک PostgreSQL آنلاین نیاز دارد. ساختن دیتابیس روی حساب شما، منابع واقعی
می‌سازد و به تأیید خودتان نیاز دارد — پس آن یک مرحله را باید در داشبورد Vercel
انجام دهید. بقیه آماده است.

---

## ۱. دیتابیس رایگان (۲ دقیقه)

در داشبورد Vercel، پروژه را باز کنید و بروید به:

**Storage → Create Database → Neon (Serverless Postgres) → Free**

Vercel خودش متغیر `DATABASE_URL` را به پروژه اضافه می‌کند. کار دیگری لازم نیست.

> پلن رایگان Neon: ۰٫۵ گیگابایت فضا. برای یک رستوران با چند سال داده فروش و
> عکس منو کاملاً کافی است.

## ۲. دو متغیر دیگر

**Settings → Environment Variables**، برای هر سه محیط
(Production / Preview / Development):

| نام | مقدار |
|---|---|
| `AUTH_SECRET` | یک رشته تصادفی حداقل ۳۲ کاراکتری |
| `NEXT_PUBLIC_APP_URL` | نشانی نهایی سایت، مثلاً `https://kajeh.vercel.app` |

برای ساختن `AUTH_SECRET`:

```bash
openssl rand -base64 32
```

**`AUTH_SECRET` عمداً مقدار پیش‌فرض ندارد.** یک رمز پیش‌فرض که به production
برسد یعنی هرکسی می‌تواند نشست جعلی بسازد و وارد پنل شود.

`NEXT_PUBLIC_APP_URL` را درست بگذارید: QR کد همین نشانی را در خود جا می‌دهد، پس
اگر بعد از چاپ عوضش کنید کدهای چاپ‌شده از کار می‌افتند.

## ۳. یک بار داده اولیه بریزید

بعد از اولین استقرار موفق، از روی کامپیوتر خودتان:

```bash
# رشته اتصال را از Vercel → Storage → .env.local کپی کنید
export DATABASE_URL="postgres://..."
npm run db:seed
```

این کار رستوران کاژه را با ۳۵ ماده اولیه، ۲۰ دستور پخت، ۱۱ غذا و ۶۰ روز فروش
نمونه می‌سازد. اگر می‌خواهید از صفر با داده‌های واقعی خودتان شروع کنید، این
مرحله را رد کنید و از پنل، مواد اولیه را وارد کنید.

## ۴. بلافاصله رمز را عوض کنید

داده نمونه با رمز `Kajeh@1404` ساخته می‌شود. اولین کاری که بعد از ورود باید
بکنید عوض کردن آن است.

---

## نکته‌ها

**مهاجرت‌ها خودکار اجرا می‌شوند.** دستور build شامل `prisma migrate deploy` است،
پس هر بار که push کنید ساختار دیتابیس هم به‌روز می‌شود.

**عکس‌ها در دیتابیس ذخیره می‌شوند، نه روی دیسک.** فایل‌سیستم Vercel فقط‌خواندنی و
موقتی است؛ هر چیزی که روی آن نوشته شود با ری‌استارت از بین می‌رود. هر عکس بعد از
فشرده‌سازی حدود ۱۰۰ کیلوبایت است. اگر روزی تعداد عکس‌ها به چند هزار رسید، فقط
`src/server/services/images.ts` باید به S3 یا Vercel Blob وصل شود.

**منطقه سرور روی `fra1` (فرانکفورت) تنظیم شده** — نزدیک‌ترین منطقه Vercel به
ایران. اگر دیتابیس را در منطقه دیگری ساختید، همان منطقه را در `vercel.json`
بگذارید تا هر درخواست نصف دنیا را دور نزند.
